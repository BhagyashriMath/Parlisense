# Backend

Express/TypeScript orchestration server for the parliamentary monitoring system.

- `server.ts` — REST APIs, WebSocket telemetry, session state, rules, alerts, approvals, and report generation
- `services/database.ts` — SQLite persistence adapter

Start from the repository root with `npm run dev`.
