/**
 * Backward compatibility re-export barrel.
 * ParliSense frontend now implements MVM (Model-View-ViewModel) architecture.
 * All domain types, schemas, and pure business algorithms reside in `src/models/`.
 */
export * from "../models";
